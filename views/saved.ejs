<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('./partials/head') %>
    <title>Document</title>
</head>
<body>
    <h1>Saved Locations</h1>
    <% user.forEach(users => { %>
        <% users.locations.forEach(location => { %>
            <p><%= location %></p>
       <% }) %>
  <%  }) %>
</body>
</html>