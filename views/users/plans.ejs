<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partials/head') %>
    <link rel="stylesheet" href="/css/users/style-plans.css">
    <title>Travel Plans</title>
</head>
<body>
    <nav>
        <%- include('../partials/nav') %>
        <a href="/locations/saved">Return to saved locations</a>
    </nav>
    <h1><%= user.locationsName %></h1>
    <p>Current Temperature: <%= data.main.temp %></p>
    <p>Weather Conditions: <%= data.weather[0].main %></p>
    <div>
    <% if ( user.travelPlan.length > 0 ) { %>
        <h4>Your Plans so Far</h4> 
        <%  } else { %>
        <h4>No plans Added Yet</h4> 
    <% }; %>
    <ul>
        <% for (let i = 0; i < user.travelPlan.length; i++){ %>
            <div class="plans">
                <li class="trav-date">
                    <%= user.travelDate[i] %>
                </li>
                <li class="trav-plan">
                    <%= user.travelPlan[i]%>
                </li>
            </div>
        <% } %>
     </ul>
    </div>
    <form action="/locations/saved/plans/<%= user._id %>?_method=POST" method="POST">
           <label>Add Plan</label> <br>
           Plan: <input type="text" name="travelPlan">
           Date: <input type="text" name="travelDate" placeholder="dd/mm/yy">
           <input type="hidden" name="locationIndex" value="<%= locationIndex %>" >
        <input type="submit" value="submit" class="btn btn-primary">
    </form>
    <%- include('../partials/footer') %>
</body>
</html>